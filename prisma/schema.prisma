generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["views"]
}

datasource db {
  provider = "mysql"
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model info__industry_analysis {
  id                                Int                                 @id @default(autoincrement()) @db.UnsignedInt
  title                             String                              @db.VarChar(50)
  summary                           String                              @db.Text
  publisher                         String?                             @db.VarChar(50)
  author                            String?                             @db.VarChar(50)
  original_file                     String?                             @db.VarChar(512)
  ai_model_version                  String?                             @db.VarChar(50)
  create_time                       DateTime?                           @default(now()) @db.DateTime(0)
  update_time                       DateTime?                           @default(now()) @updatedAt @db.DateTime(0)
  original_url                      String?                             @db.VarChar(512)
  report_time                       DateTime?                           @db.DateTime(0)
  relation__board_industry_analysis relation__board_industry_analysis[]

  @@index([create_time], map: "idx_create_time")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model info__stock_board {
  id                                Int                                 @id @default(autoincrement()) @db.UnsignedInt
  board_name                        String                              @unique(map: "idx_board_name") @db.VarChar(50)
  create_time                       DateTime                            @default(now()) @db.DateTime(0)
  update_time                       DateTime                            @default(now()) @updatedAt @db.DateTime(0)
  relation__board_industry_analysis relation__board_industry_analysis[]
  relation__stock_board_company     relation__stock_board_company[]
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model info__stock_company {
  id                             Int                              @id @default(autoincrement()) @db.UnsignedInt
  company_name                   String                           @unique(map: "idx_company_name") @db.VarChar(50)
  company_code                   String                           @unique(map: "idx_company_code") @db.VarChar(20)
  company_akshare_code           String                           @unique(map: "idx_company_akshare_code") @db.VarChar(20)
  create_time                    DateTime                         @default(now()) @db.DateTime(0)
  update_time                    DateTime                         @default(now()) @updatedAt @db.DateTime(0)
  industry                       String?                          @db.VarChar(20)
  ipo_date                       DateTime?                        @db.Date
  circulating_shares             Decimal?                         @db.Decimal(20, 2)
  total_shares                   Decimal?                         @db.Decimal(20, 2)
  quote__balance_sheet           quote__balance_sheet[]
  quote__cash_flow_sheet         quote__cash_flow_sheet[]
  quote__profit_sheet            quote__profit_sheet[]
  quote__stock_constituent_daily quote__stock_constituent_daily[]
  relation__stock_board_company  relation__stock_board_company[]
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model info__tweet {
  id            BigInt   @id @default(autoincrement()) @db.UnsignedBigInt
  tweet_id      String   @unique(map: "uk_tweet_id") @db.VarChar(64)
  user_name     String   @db.VarChar(128)
  tweet_date    DateTime @db.DateTime(0)
  tweet_text    String   @db.Text
  reply_count   String   @db.VarChar(20)
  retweet_count String   @db.VarChar(20)
  like_count    String   @db.VarChar(20)
  view_count    String   @db.VarChar(20)
  tweet_url     String   @db.VarChar(512)
  tweet_from    String   @db.VarChar(32)
  collect_from  String   @db.VarChar(512)
  create_time   DateTime @default(now()) @db.DateTime(0)
  update_time   DateTime @default(now()) @updatedAt @db.DateTime(0)

  @@index([tweet_date], map: "idx_tweet_date")
  @@index([user_name], map: "idx_user_name")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model quote__stock_constituent_daily {
  id                  BigInt              @id @default(autoincrement()) @db.UnsignedBigInt
  company_id          Int                 @db.UnsignedInt
  trade_date          DateTime            @db.Date
  volume              BigInt
  turnover_rate       Decimal             @db.Decimal(6, 3)
  change_percent      Decimal             @db.Decimal(5, 2)
  price_range         Decimal             @db.Decimal(10, 2)
  create_time         DateTime            @default(now()) @db.DateTime(0)
  update_time         DateTime            @default(now()) @updatedAt @db.DateTime(0)
  hfq_change_amt      Decimal             @db.Decimal(10, 2)
  hfq_close_price     Decimal             @db.Decimal(10, 2)
  hfq_high_price      Decimal             @db.Decimal(10, 2)
  hfq_low_price       Decimal             @db.Decimal(10, 2)
  hfq_open_price      Decimal             @db.Decimal(10, 2)
  hfq_turnover        Decimal             @db.Decimal(18, 2)
  none_change_amt     Decimal             @db.Decimal(10, 2)
  none_close_price    Decimal             @db.Decimal(10, 2)
  none_high_price     Decimal             @db.Decimal(10, 2)
  none_low_price      Decimal             @db.Decimal(10, 2)
  none_open_price     Decimal             @db.Decimal(10, 2)
  none_turnover       Decimal             @db.Decimal(18, 2)
  qfq_change_amt      Decimal             @db.Decimal(10, 2)
  qfq_close_price     Decimal             @db.Decimal(10, 2)
  qfq_high_price      Decimal             @db.Decimal(10, 2)
  qfq_low_price       Decimal             @db.Decimal(10, 2)
  qfq_open_price      Decimal             @db.Decimal(10, 2)
  qfq_turnover        Decimal             @db.Decimal(18, 2)
  info__stock_company info__stock_company @relation(fields: [company_id], references: [id], map: "fk_quote_company_id")

  @@unique([company_id, trade_date], map: "uk_company_trade_date")
  @@index([trade_date], map: "idx_trade_date")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model relation__board_industry_analysis {
  id                      Int                     @id @default(autoincrement()) @db.UnsignedInt
  board_id                Int                     @db.UnsignedInt
  industry_analysis_id    Int                     @db.UnsignedInt
  update_time             DateTime                @default(now()) @updatedAt @db.DateTime(0)
  create_time             DateTime                @default(now()) @db.DateTime(0)
  info__stock_board       info__stock_board       @relation(fields: [board_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "relation__stock_board_industry_analysis_ibfk_1")
  info__industry_analysis info__industry_analysis @relation(fields: [industry_analysis_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "relation__stock_board_industry_analysis_ibfk_2")

  @@unique([board_id, industry_analysis_id], map: "board_id_industry_analysis_id")
  @@index([board_id], map: "board_id")
  @@index([industry_analysis_id], map: "industry_analysis_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model relation__stock_board_company {
  id                  Int                 @id @default(autoincrement()) @db.UnsignedInt
  company_id          Int                 @db.UnsignedInt
  update_time         DateTime            @default(now()) @updatedAt @db.DateTime(0)
  board_id            Int                 @db.UnsignedInt
  create_time         DateTime            @default(now()) @db.DateTime(0)
  weight              Int                 @default(0) @db.UnsignedInt
  info__stock_board   info__stock_board   @relation(fields: [board_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "relation__stock_board_company_ibfk_1")
  info__stock_company info__stock_company @relation(fields: [company_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "relation__stock_board_company_ibfk_2")

  @@unique([company_id, board_id], map: "company_id")
  @@index([board_id], map: "board_id")
  @@index([company_id], map: "company_id_2")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model summary__article {
  id                          BigInt                        @id @default(autoincrement()) @db.UnsignedBigInt
  title                       String                        @db.VarChar(255)
  summary                     String                        @db.Text
  source_url                  String                        @unique(map: "uk_source_url") @db.VarChar(512)
  tags                        String?                       @db.VarChar(255)
  create_time                 DateTime                      @default(now()) @db.DateTime(0)
  update_time                 DateTime                      @default(now()) @updatedAt @db.DateTime(0)
  publication                 String?                       @db.VarChar(20)
  issue_date                  DateTime?                     @db.Date
  source_text                 String?                       @db.Text
  contributor                 String?                       @db.VarChar(255)
  info__predicts              info__predict[]
  relation__industry_articles relation__industry_article[]

  @@index([tags], map: "idx_tags")
  @@index([title], map: "idx_title")
}

/// 行业表
model info__industry {
  id                          Int                           @id @default(autoincrement()) @db.UnsignedInt
  name                        String                        @unique(map: "uk_industry_name") @db.VarChar(100)
  description                 String?                       @db.Text
  create_time                 DateTime                      @default(now()) @db.DateTime(0)
  update_time                 DateTime                      @default(now()) @updatedAt @db.DateTime(0)
  relation__industry_articles relation__industry_article[]
}

/// 行业-文章关联表
model relation__industry_article {
  id               Int              @id @default(autoincrement()) @db.UnsignedInt
  industry_id      Int              @db.UnsignedInt
  article_id       BigInt           @db.UnsignedBigInt
  create_time      DateTime         @default(now()) @db.DateTime(0)
  update_time      DateTime         @default(now()) @updatedAt @db.DateTime(0)
  info__industry   info__industry   @relation(fields: [industry_id], references: [id], onDelete: Cascade, map: "fk_industry_article_industry_id")
  summary__article summary__article @relation(fields: [article_id], references: [id], onDelete: Cascade, map: "fk_industry_article_article_id")

  @@unique([industry_id, article_id], map: "uk_industry_article")
  @@index([industry_id], map: "idx_industry_id")
  @@index([article_id], map: "idx_article_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model summary__tweet {
  id           Int      @id @default(autoincrement()) @db.UnsignedInt
  collect_from String   @db.VarChar(128)
  summary      String   @db.Text
  create_time  DateTime @default(now()) @db.DateTime(0)
  update_time  DateTime @default(now()) @updatedAt @db.DateTime(0)
  date         DateTime @db.Date

  @@unique([collect_from, date], map: "collect_from")
  @@index([collect_from], map: "idx_collect_from")
  @@index([date], map: "idx_date")
}

/// 预测记录表
model info__predict {
  id               BigInt                           @id @default(autoincrement()) @db.UnsignedBigInt
  create_time      DateTime                         @default(now()) @db.DateTime(0)
  update_time      DateTime                         @default(now()) @updatedAt @db.DateTime(0)
  content          String                           @db.Text
  proposed_at      DateTime                         @db.Date
  interval_start   DateTime                         @db.Date
  interval_end     DateTime                         @db.Date
  verify_status    VerifyStatus                     @default(not_due)
  delayed_note     String?                          @db.Text
  assoc_type       AssocType?
  assoc_article_id BigInt?                          @db.UnsignedBigInt
  assoc_link       String?                          @db.VarChar(2048)
  predictor        String?                          @db.VarChar(50)
  observations     indicator__predict_observation[]
  summary__article summary__article?                @relation(fields: [assoc_article_id], references: [id])

  @@index([interval_start], map: "idx_interval_start")
  @@index([interval_end], map: "idx_interval_end")
  @@index([proposed_at], map: "idx_proposed_at")
  @@index([verify_status], map: "idx_verify_status")
  @@index([predictor], map: "idx_predictor")
  @@index([assoc_article_id], map: "info__predict_assoc_article_id_fkey")
}

/// 预测观察记录表
model indicator__predict_observation {
  id               BigInt        @id @default(autoincrement()) @db.UnsignedBigInt
  predict_id       BigInt        @db.UnsignedBigInt
  observation_date DateTime      @db.Date
  content          String        @db.Text
  create_time      DateTime      @default(now()) @db.DateTime(0)
  update_time      DateTime      @default(now()) @updatedAt @db.DateTime(0)
  info__predict    info__predict @relation(fields: [predict_id], references: [id], onDelete: Cascade)

  @@index([predict_id], map: "idx_predict_id")
  @@index([observation_date], map: "idx_observation_date")
}

/// 资产负债表
model quote__balance_sheet {
  id                  BigInt              @id @default(autoincrement()) @db.UnsignedBigInt
  company_id          Int                 @db.UnsignedInt
  report_date         DateTime            @db.Date
  total_parent_equity Decimal             @db.Decimal(20, 2)
  create_time         DateTime            @default(now()) @db.DateTime(0)
  update_time         DateTime            @default(now()) @updatedAt @db.DateTime(0)
  info__stock_company info__stock_company @relation(fields: [company_id], references: [id], map: "fk_balance_sheet_company_id")

  @@unique([company_id, report_date], map: "uk_balance_company_report_date")
  @@index([report_date], map: "idx_balance_report_date")
}

/// 利润表
model quote__profit_sheet {
  id                   BigInt              @id @default(autoincrement()) @db.UnsignedBigInt
  company_id           Int                 @db.UnsignedInt
  report_date          DateTime            @db.Date
  operate_income       Decimal             @db.Decimal(20, 2)
  parent_netprofit     Decimal             @db.Decimal(20, 2)
  create_time          DateTime            @default(now()) @db.DateTime(0)
  update_time          DateTime            @default(now()) @updatedAt @db.DateTime(0)
  netprofit            Decimal             @db.Decimal(20, 2)
  operate_cost         Decimal             @db.Decimal(20, 2)
  total_operate_cost   Decimal             @db.Decimal(20, 2)
  total_operate_income Decimal             @db.Decimal(20, 2)
  info__stock_company  info__stock_company @relation(fields: [company_id], references: [id], map: "fk_profit_sheet_company_id")

  @@unique([company_id, report_date], map: "uk_profit_company_report_date")
  @@index([report_date], map: "idx_profit_report_date")
}

/// 现金流量表
model quote__cash_flow_sheet {
  id                  BigInt              @id @default(autoincrement()) @db.UnsignedBigInt
  company_id          Int                 @db.UnsignedInt
  report_date         DateTime            @db.Date
  netcash_operate     Decimal             @db.Decimal(20, 2)
  netcash_invest      Decimal             @db.Decimal(20, 2)
  netcash_finance     Decimal             @db.Decimal(20, 2)
  rate_change_effect  Decimal             @db.Decimal(20, 2)
  capex               Decimal            @db.Decimal(20, 2)
  create_time         DateTime            @default(now()) @db.DateTime(0)
  update_time         DateTime            @default(now()) @updatedAt @db.DateTime(0)
  info__stock_company info__stock_company @relation(fields: [company_id], references: [id], map: "fk_cash_flow_sheet_company_id")

  @@unique([company_id, report_date], map: "uk_cash_flow_company_report_date")
  @@index([report_date], map: "idx_cash_flow_report_date")
}

/// 财务报表预测指标表
model indicator__predict_financial_report {
  id                    BigInt   @id @default(autoincrement()) @db.UnsignedBigInt
  company_id            Int?     @db.UnsignedInt
  board_id              Int?     @db.UnsignedInt
  report_date           DateTime @db.Date
  parent_netprofit      Decimal? @db.Decimal(20, 2)
  total_parent_equity   Decimal? @db.Decimal(20, 2)
  operate_income        Decimal? @db.Decimal(20, 2)
  netcash_operate       Decimal? @db.Decimal(20, 2)
  create_time           DateTime @default(now()) @db.DateTime(0)
  update_time           DateTime @default(now()) @updatedAt @db.DateTime(0)
  dividend_payout_ratio Decimal? @db.Decimal(8, 4)

  @@index([company_id], map: "idx_company_id")
  @@index([board_id], map: "idx_board_id")
  @@index([report_date], map: "idx_report_date")
}

view view_financial_statements {
  company_id                     Int?      @db.UnsignedInt
  report_date                    DateTime? @db.Date
  total_shares                   Decimal?  @db.Decimal(20, 2)
  total_parent_equity            Decimal?  @db.Decimal(20, 2)
  total_operate_income_ttm       Decimal?  @db.Decimal(42, 2)
  total_operate_income_last_year Decimal?  @db.Decimal(42, 2)
  operate_income_ttm             Decimal?  @db.Decimal(42, 2)
  operate_income_last_year       Decimal?  @db.Decimal(42, 2)
  total_operate_cost_ttm         Decimal?  @db.Decimal(42, 2)
  total_operate_cost_last_year   Decimal?  @db.Decimal(42, 2)
  operate_cost_ttm               Decimal?  @db.Decimal(42, 2)
  operate_cost_last_year         Decimal?  @db.Decimal(42, 2)
  netprofit_ttm                  Decimal?  @db.Decimal(42, 2)
  netprofit_last_year            Decimal?  @db.Decimal(42, 2)
  parent_netprofit_ttm           Decimal?  @db.Decimal(42, 2)
  parent_netprofit_last_year     Decimal?  @db.Decimal(42, 2)
  netcash_operate_ttm            Decimal?  @db.Decimal(42, 2)
  netcash_operate_last_year      Decimal?  @db.Decimal(42, 2)
  netcash_invest_ttm             Decimal?  @db.Decimal(42, 2)
  netcash_invest_last_year       Decimal?  @db.Decimal(42, 2)
  netcash_finance_ttm            Decimal?  @db.Decimal(42, 2)
  netcash_finance_last_year      Decimal?  @db.Decimal(42, 2)
  rate_change_effect_ttm         Decimal?  @db.Decimal(42, 2)
  rate_change_effect_last_year   Decimal?  @db.Decimal(42, 2)
  free_cash_flow_ttm             Decimal?  @db.Decimal(42, 2)
  free_cash_flow_last_year       Decimal?  @db.Decimal(42, 2)
}

enum VerifyStatus {
  implemented
  not_implemented
  partial
  not_due
  delayed
}

enum AssocType {
  article
  link
}
